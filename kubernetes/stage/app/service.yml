apiVersion: v1
kind: Service
metadata:
  name: diffaction-events-service
spec:
  selector:
    app: diffaction-events-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: web
